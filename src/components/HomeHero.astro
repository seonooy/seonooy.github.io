---
interface LinkItem {
  label: string;
  href: string;
}

interface Props {
  name: string;
  role: string;
  intro: string;
  links: LinkItem[];
}

const { name, role, intro, links } = Astro.props;
---

<section class="hero-block">
  <h1>{name}</h1>
  <p class="hero-role">{role}</p>
  <p class="hero-intro">{intro}</p>
  <div class="hero-links">
    {links.map((link) => (
      <a href={link.href} target={link.href.startsWith("http") ? "_blank" : undefined} rel="noreferrer">
        {link.label}
      </a>
    ))}
  </div>
</section>
